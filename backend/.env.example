# ===========================================
# PDF to DOCX Converter - Environment Variables
# ===========================================

# Application
APP_ENV=development
APP_DEBUG=true
APP_SECRET_KEY=your-secret-key-change-in-production

# API Keys - OCR Services
MISTRAL_API_KEY=your-mistral-api-key
DEEPSEEK_API_KEY=your-deepseek-api-key

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pdf_converter

# Redis (for Celery queue)
REDIS_URL=redis://localhost:6379/0

# Storage (S3-compatible)
STORAGE_TYPE=local
# For S3/MinIO:
# STORAGE_TYPE=s3
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET=pdf-converter-files
# S3_REGION=us-east-1

# File Storage Path (for local storage)
STORAGE_PATH=./storage

# Processing Settings
MAX_FILE_SIZE_MB=100
MAX_PAGES_PER_JOB=200
MAX_CONCURRENT_JOBS=5

# OCR Settings
DEFAULT_OCR_PROVIDER=mistral
# Options: mistral, deepseek, surya (local)
OCR_TIMEOUT_SECONDS=300

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate Limiting
RATE_LIMIT_PER_MINUTE=30

# Logging
LOG_LEVEL=INFO
