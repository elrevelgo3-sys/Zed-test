# 🚀 Деплой на Railway — Пошаговая инструкция

## Что такое Railway?

Railway — это платформа для деплоя приложений (как Heroku, но проще и дешевле).
Бесплатный тариф: $5 кредитов в месяц (хватит на тестирование).

---

## Подготовка

### 1. Создайте аккаунт GitHub

Если ещё нет — создайте на [github.com](https://github.com)

### 2. Загрузите код на GitHub

```bash
# В папке проекта
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/pdf-converter.git
git branch -M main
git push -u origin main
```

### 3. Получите API ключ Mistral

1. Зайдите на [console.mistral.ai](https://console.mistral.ai)
2. Создайте аккаунт
3. Перейдите в API Keys → Create new key
4. Скопируйте ключ (начинается с `sk-...`)

---

## Деплой на Railway

### Шаг 1: Создайте аккаунт Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Login" → "Login with GitHub"
3. Авторизуйте Railway

### Шаг 2: Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `pdf-converter`
4. Railway автоматически обнаружит Dockerfile

### Шаг 3: Настройте переменные окружения

В Railway dashboard:

1. Откройте ваш сервис (кликните на него)
2. Перейдите во вкладку **"Variables"**
3. Добавьте переменные:

```
MISTRAL_API_KEY=sk-ваш-ключ-от-mistral
APP_SECRET_KEY=сгенерируйте-случайную-строку-32-символа
APP_ENV=production
APP_DEBUG=false
STORAGE_TYPE=local
STORAGE_PATH=/app/storage
```

### Шаг 4: Добавьте Redis (для очередей)

1. В проекте нажмите **"New"** → **"Database"** → **"Add Redis"**
2. Railway автоматически создаст переменную `REDIS_URL`
3. Эта переменная автоматически станет доступна вашему приложению

### Шаг 5: Настройте порт

Railway автоматически назначает порт через переменную `PORT`.
Убедитесь, что в Dockerfile команда запуска использует `$PORT`:

```dockerfile
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

### Шаг 6: Деплой!

1. Railway автоматически начнёт сборку после добавления переменных
2. Следите за логами во вкладке **"Deployments"**
3. После успешного деплоя появится URL вида: `https://pdf-converter-production.up.railway.app`

---

## Деплой Frontend (отдельно)

### Вариант 1: На том же Railway

1. В проекте нажмите **"New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий
3. В настройках укажите **Root Directory**: `frontend`
4. Добавьте переменную: `VITE_API_URL=https://ваш-backend-url.railway.app`

### Вариант 2: На Vercel (рекомендуется для frontend)

1. Зайдите на [vercel.com](https://vercel.com)
2. Импортируйте репозиторий
3. В настройках:
   - **Root Directory**: `frontend`
   - **Environment Variables**: `VITE_API_URL=https://ваш-backend-url`
4. Deploy!

---

## Проверка работоспособности

### 1. Проверьте API

```bash
# Замените URL на ваш
curl https://pdf-converter-production.up.railway.app/health
```

Должно вернуть:
```json
{"status": "healthy", "version": "1.0.0", "ocr_available": true}
```

### 2. Проверьте конвертацию

```bash
curl -X POST https://pdf-converter-production.up.railway.app/api/v1/convert \
  -F "file=@test.pdf" \
  -F "ocr_enabled=true"
```

### 3. Откройте Swagger документацию

```
https://pdf-converter-production.up.railway.app/docs
```

---

## Частые проблемы

### ❌ "No module named 'app'"

**Причина:** Неправильная структура проекта или PYTHONPATH.

**Решение:** Убедитесь, что в Dockerfile:
```dockerfile
WORKDIR /app
COPY . .
```

### ❌ "Mistral API key required"

**Причина:** Не установлена переменная `MISTRAL_API_KEY`.

**Решение:** Добавьте в Variables на Railway.

### ❌ Build timeout

**Причина:** Долгая установка зависимостей.

**Решение:** Railway даёт 20 минут на сборку. Если не хватает — оптимизируйте requirements.txt.

### ❌ "Port already in use"

**Причина:** Приложение не использует переменную PORT.

**Решение:** Измените CMD на:
```dockerfile
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
```

---

## Мониторинг

### Логи

В Railway dashboard → Deployments → View Logs

### Метрики

Railway показывает:
- CPU usage
- Memory usage
- Network traffic

### Alerts

Настройте в Settings → Notifications

---

## Стоимость

### Railway Pricing

| Ресурс | Бесплатно | Hobby ($5/мес) |
|--------|-----------|----------------|
| CPU | 500 часов/мес | Безлимит |
| RAM | 512MB | 8GB |
| Хранилище | 1GB | 100GB |

### Mistral API Pricing

| Модель | Input | Output |
|--------|-------|--------|
| Pixtral Large | $2/1M tokens | $6/1M tokens |

**Примерно:** 1000 страниц документов ≈ $2-5

---

## Что дальше?

1. ✅ Настроить кастомный домен
2. ✅ Добавить SSL (Railway делает автоматически)
3. ✅ Настроить автоскейлинг
4. ✅ Добавить мониторинг ошибок (Sentry)

---

## Нужна помощь?

Если что-то не работает — напишите мне с:
1. Скриншотом ошибки
2. Логами из Railway
3. Названием шага, на котором застряли

Я помогу разобраться! 🚀